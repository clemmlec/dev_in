include:
  - UserFixtures.yaml
  - TagFixtures.yaml

App\Entity\Meme:
  meme_{1..20}:
    nom: "<words(4, true)>"
    description: "<generateLoremDesct()>"
    user_id: "@userAdmin"
    categories_id: "<randomElements(@categorie_{1..10})>"
    createdAt: "<generateDate()>"