{% for user in follow %}
     {# pour homogéniser les datas #}
     {% if follower == false %}
          {% set user = user.friend %}
     {% else %}
          {% set user = user.user %}
     {% endif %}
     
     {{ include('Components/_user.html.twig')}}
{% endfor %}