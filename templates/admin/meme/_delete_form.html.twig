<form method="post" action="{{ path('app_meme_delete', {'id': meme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meme.id) }}">
    <button class="btn">Delete</button>
</form>
