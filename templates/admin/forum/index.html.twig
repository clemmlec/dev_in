{% extends 'base.html.twig' %}

{% block title %}Forum index{% endblock %}

{% block body %}
    <h1>Forum index</h1>
    <section class="d-flex">
        {% for cat in forums %}
        <div class="card-subject demiDiv">
            <h1 class="toptitre left {{cat.style}}">
                    {{cat.nom}}
                        {# <img 
                            class="avatar" 
                            src="{{ user.imageName ? vich_uploader_asset(user, 'imageFile') : "https://fakeimg.pl/200x100" }}" 
                            alt="{user.name}}"
                        > #}
                {# <a href="{{ path('app_forum_show', {'id': cat.id}) }}">show</a> #}
            </h1> 
            <a href="{{ path('app_forum_edit', {'id': cat.id}) }}">edit</a>
            
        </div>
        {% else %}
            <tr>
                <td colspan="10">no records found</td>
            </tr>
            
        
        {% endfor %}
    </section>


    <a href="{{ path('app_forum_new') }}">Create new</a>
{% endblock %}
